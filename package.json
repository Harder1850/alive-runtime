{
  "name": "alive-runtime",
  "version": "0.1.0",
  "description": "Governance and orchestration layer of ALIVE. Controls routing, STG, scheduling, enforcement, and lifecycle.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/red-team.test.ts",
    "start": "node --import tsx src/main.ts",
    "start:bridge": "node --import tsx src/wiring/start-bridge.ts",
    "slice1": "node --import tsx src/wiring/slice1-cycle.ts",
    "slice2": "node --import tsx src/wiring/slice2-demo.ts",
    "slice3": "node --import tsx src/wiring/slice3-demo.ts"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "systeminformation": "^5.31.5",
    "ws": "^8.20.0"
  }
}
