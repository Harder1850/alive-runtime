/** Checks action admissibility against Constitution policy. */
export class AdmissibilityCheck {
  check(_action: unknown): boolean {
    // TODO: implement using Constitution AdmissibilityPolicy
    return true;
  }
}
